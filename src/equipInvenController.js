// const {nanoid} = require("nanoid");
// const { MessageChannel } = require("worker_threads");



// function createEquipment(name, priceInCents, quantity) {
//     console.log(name)
//     const name = id || nanoid();
//     const priceInCents = Math.ceil(amount);
//     const quantity = (roundDonation - amount).toFixed(2);
//     return  `${ewItem}` = {
//       purchaseId,
//       name,
//       priceInCents,
//       quantity,
//       MessageChannel
//     }
    
//     };
   
  
// console.log(createEquipment())

// function shoppingCart(){

// }

// function listEquipment(equipment) {
//     return  equipment.map(({id, name, priceInCents, inStock, purchaseDate}) => ({
//     id,
//     name,
//     amount,
//     donation}));
//     }